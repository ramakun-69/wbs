version: "3.9"

services:
  wbs-app:
    container_name: wbs-app
    image: wbs:latest
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    working_dir: /var/www/
    restart: unless-stopped
    tty: true
    env_file:
      - .env
    volumes:
      - ./:/var/www/
    healthcheck:
      test: ["CMD", "php", "-v"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - default
      - proxy

  wbs-nginx:
    container_name: wbs-nginx
    image: nginx:stable-alpine
    restart: unless-stopped
    depends_on:
      - wbs-app
    ports:
    - "8003:80"
    volumes:
      - ./:/var/www/
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - default
      - proxy

  wbs-queue:
    container_name: wbs-queue
    image: wbs:latest
    working_dir: /var/www/
    restart: unless-stopped
    env_file:
      - .env
    command: >
      php artisan queue:work redis
      --sleep=3
      --tries=3
      --timeout=300
      --max-time=3600
    volumes:
      - ./:/var/www/
    depends_on:
      - wbs-app
    networks:
      - default
      - proxy

  wbs-scheduler:
    container_name: wbs-scheduler
    image: wbs:latest
    working_dir: /var/www/
    restart: unless-stopped
    env_file:
      - .env
    command:
      - php
      - artisan
      - schedule:work
    volumes:
      - ./:/var/www/
    depends_on:
      - wbs-app
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true